pre-commit:
  commands:
    biome:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: ./node_modules/.bin/biome check --write {staged_files}
      stage_fixed: true

commit-msg:
  scripts:
    "conventional-commit.sh":
      runner: sh
